import { ShowUserrolePipe } from './show-userrole.pipe';

describe('ShowUserrolePipe', () => {
  it('create an instance', () => {
    const pipe = new ShowUserrolePipe();
    expect(pipe).toBeTruthy();
  });
});
